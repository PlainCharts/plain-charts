{
  "comment": "Own toolchain for the engine terminal (React + Ink). Separate from the app's tsconfig so React/JSX never enters the app or the engine's zero-dep core. tsx reads jsx from here.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022"],
    "allowJs": true,
    "checkJs": false,
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true
  },
  "include": ["src/**/*.jsx", "test/**/*.jsx"],
  "exclude": ["node_modules"]
}
